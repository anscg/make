import { OnboardingDialog } from "@/app/onboarding/OnboardingDialog"
// TEMPORARY preview route — delete before commit.
export default function Preview() {
  return <main className="min-h-svh"><OnboardingDialog name="Anson" /></main>
}
